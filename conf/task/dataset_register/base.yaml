# @package _global_
task:
  name: dataset_register
  stage: "01_dataset_register"
  project_name: "${run.clearml.project_root}/${run.clearml.project_layout.solution_root}/${run.usecase_id}/${run.clearml.project_layout.group_map.dataset_register}"
# ClearML Task 名は CODE 側で規約に従って組み立てる（docs/03 を参照）
