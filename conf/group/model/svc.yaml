# Model variant: SVC (classification only)
model_variant:
  name: svc
  framework: sklearn
  task_type: classification
  class_path: sklearn.svm.SVC
  params:
    kernel: rbf
    C: 1.0
    gamma: scale
    max_iter: 1000
