# Model variant: SVR (regression only)
model_variant:
  name: svr
  framework: sklearn
  task_type: regression
  class_path: sklearn.svm.SVR
  params:
    kernel: rbf
    C: 1.0
    gamma: scale
    max_iter: 1000
